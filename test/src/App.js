import { useState } from 'react';

//인풋창이있고 버튼이 있다.
//인풋창에 값을 입력하고 버튼을 클릭하면 아이템이 추가가 된다.
//아이템 삭제버튼을 누르면 삭제가 가능하다

function App() {
  const  [inputValue.setInterval]=useState('')
  const
  const addItem=()=>{

  }
  return (
    <main>
      <input value={importValue} type="text" onChange=
      {(event)=>setInputValue(event.target.inputValue)} />
      <button onClick={addItem}>추가</button>
    </main>
  )
}

export default App;